# Галерея "Любовь" — Проект на Next.js

Премиальный сайт для галереи интерьерного дизайна, разработанный на **Next.js 15 (App Router)** с использованием **Tailwind CSS** и **Framer Motion**.

Проект оптимизирован для развертывания на обычном PHP-хостинге (например, Beget) через статический экспорт.

---

## 🛠 Быстрый старт (Локально)

1.  **Установка зависимостей:**
    ```bash
    npm install
    ```
2.  **Запуск сервера разработки:**
    ```bash
    npm run dev
    ```
    Сайт будет доступен по адресу: `http://localhost:3000`.

---

## 🚀 Сборка и Деплой (на хостинг)

Проект настроен на **статический экспорт**. Это значит, что после сборки вы получаете готовые HTML/JS/CSS файлы, которые работают на любом сервере.

### Автоматическая сборка (Рекомендуется для Windows)

Я подготовил скрипты, которые сами соберут проект и упакуют его в готовый архив:

*   **Если вы в Git Bash (MinGW):**
    ```bash
    sh build-and-zip.sh
    ```
*   **Если вы просто в Windows (через CMD):**
    Двойной клик по файлу `build-and-zip.bat`.

**Результат:** В корне проекта появится файл `deploy.zip`.

### Как загрузить на Beget / PHP Хостинг

1.  Загрузите файл `deploy.zip` в корень вашего сайта (обычно папка `public_html`).
2.  Распакуйте его.
3.  **Важно про права доступа:** Если после загрузки сайт выдает ошибку *403 Forbidden* или не грузит картинки, откройте в браузере:
    `ваш-сайт.ru/fix-perms.php`
    Этот скрипт автоматически выставит правильные права (755 для папок, 644 для файлов).

---

## 📧 Настройка почты (Формы связи)

Все формы на сайте (Контакты, Консультация, Заказ) отправляются через файл `public/send.php`.

**Чтобы изменить Email получателя:**
1.  Откройте `public/send.php`.
2.  Найдите строку 9: `$toEmail = "moraamethyst@virgilian.com";`.
3.  Замените адрес на ваш.

---

## 📁 Структура папок

*   `app/` — Страницы и роутинг (App Router).
*   `components/` — Повторно используемые UI компоненты.
*   `data/` — Весь контент: каталог товаров, тексты страниц, статьи.
*   `public/` — Статика и серверные скрипты (`.htaccess`, `send.php`, `fix-perms.php`).
*   `lib/` — Утилиты для валидации форм и форматирования данных.

---

## 📝 Основные правки (Январь 2026)

*   **Дизайн:** Убраны кастомные курсоры, увеличены шрифты в Hero-секции, замедлена анимация marquee.
*   **Каталог:** Переделан блок "Мебель для животных" (4 колонки), добавлена секция "Коллекции" списком.
*   **Функционал:** Реализована отправка форм через PHP, добавлены маски телефонов, исправлены баги футера.
*   **О нас:** Добавлено видео-превью с модальным окном для блока "Азбука".
